# Build outputs
dist/
.next/
out/
build/

# Generated
libs/db/src/generated/
node_modules/
.pnpm-store/

# Nx cache
.nx/cache/
.nx/workspace-data/

# Misc
coverage/
*.lock
pnpm-lock.yaml

# Design reference bundle — hand-authored HTML/CSS/JS mocks kept verbatim (see docs/design/DESIGN-NOTES.md)
docs/design/

# Prompt templates + fixtures — literal frontmatter+body content, parsed byte-for-byte and asserted
# in tests. Prettier's markdown formatter inserts a blank line after the frontmatter fence and a
# trailing newline, corrupting that content — so every .md under prompts/ (templates + fixtures) is
# left unformatted. The glob auto-covers new templates; the sibling .ts files stay formatted.
libs/ai/src/prompts/**/*.md
