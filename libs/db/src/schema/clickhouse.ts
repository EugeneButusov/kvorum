// ClickHouseDatabase interface — populated in PR-2 (ingestion domain).
// Per-source archive tables are composed in from libs/sources/<name>/src/schema/.
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ClickHouseDatabase {}
