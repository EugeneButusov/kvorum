{
  "name": "indexer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ../../dist/apps/indexer/main.js",
    "start:dev": "pnpm run build:dev && node ../../dist/apps/indexer/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.app.json",
    "build": "webpack-cli build --node-env=production",
    "build:dev": "webpack-cli build --node-env=development",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "pg-boss": "^12.18.2"
  }
}
