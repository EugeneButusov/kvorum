{
  "name": "@nest/forum",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.lib.json",
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@libs/chain": "workspace:*",
    "@libs/db": "workspace:*",
    "@nest/chain": "workspace:*",
    "@sources/core": "workspace:*",
    "@sources/forum": "workspace:*",
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.0.0"
  }
}
