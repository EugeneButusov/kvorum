{
  "name": "@nest/sources-core",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.lib.json",
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src"
  },
  "dependencies": {
    "@libs/observability": "workspace:*",
    "@sources/core": "workspace:*",
    "tslib": "^2.3.0"
  }
}
